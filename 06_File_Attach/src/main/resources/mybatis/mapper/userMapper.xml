<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.userMapper">

    <select id="getAllUsers" resultType="UserDTO">
        select uid, email, file_path, original_filename, filesystem_name
        from tbl_user
        order by uid desc
        limit 0,10
    </select>

    <select id="getUserById" parameterType="_int" resultType="UserDTO">
        select uid, email, file_path, original_filename, filesystem_name
        from tbl_user
        where uid = #{uid}
    </select>

    <insert id="insertUser" parameterType="UserDTO">
        insert into tbl_user(email, file_path, original_filename, filesystem_name)
        values (#{email},#{filePath},#{originalFilename},#{filesystemName})
    </insert>


</mapper>
